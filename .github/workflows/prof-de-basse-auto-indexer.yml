name: 🎸 Prof de Basse - Auto Indexer FIXED

on:
  push:
    branches: [ main, master ]
    paths:
      - 'Methodes/**'
      - 'Exercices/**'
      - '**.mp3'
      - '**.pdf'
      - '**.png'
      - '**.json'
  workflow_dispatch:

jobs:
  auto-index:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: 📦 Install dependencies (optional)
        run: |
          python -m pip install --upgrade pip
          # Note: json, pathlib, re sont des modules built-in, pas besoin de les installer
          echo "✅ Python standard library ready (json, pathlib, re, os)"
      
      - name: 🔍 Verify repository structure
        run: |
          echo "📁 Checking scripts directory..."
          ls -la scripts/ || echo "⚠️  scripts/ not found"
          echo ""
          echo "📁 Checking Methodes directory..."
          ls -la Methodes/ | head -20 || echo "⚠️  Methodes/ not found"
      
      - name: 🔧 Make script executable
        run: |
          chmod +x scripts/extract_musical_metadata.py
      
      - name: 🎵 Extract musical metadata
        id: extract
        run: |
          echo "🎸 Running metadata extraction..."
          python scripts/extract_musical_metadata.py
      
      - name: 📊 Verify extraction results
        run: |
          if [ -f "resources/complete-resource-map.json" ]; then
            echo "✅ JSON generated successfully"
            echo ""
            echo "📄 File info:"
            ls -lh resources/complete-resource-map.json
            echo ""
            echo "📊 Statistics:"
            cat resources/stats.json
          else
            echo "❌ JSON file not generated"
            exit 1
          fi
      
      - name: 📝 Create workflow summary
        if: always()
        run: |
          echo "# 🎸 Prof de Basse - Indexation Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f "resources/stats.json" ]; then
            echo "## ✅ Extraction réussie!" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 📊 Statistiques" >> $GITHUB_STEP_SUMMARY
            echo '```json' >> $GITHUB_STEP_SUMMARY
            cat resources/stats.json >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          else
            echo "## ❌ Erreur d'extraction" >> $GITHUB_STEP_SUMMARY
            echo "Vérifier les logs ci-dessus." >> $GITHUB_STEP_SUMMARY
          fi
      
      - name: 💾 Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "Prof de Basse Bot 🎸"
          
          git add resources/
          
          if git diff --staged --quiet; then
            echo "ℹ️  Pas de changements à commiter"
          else
            git commit -m "🤖 Auto-update: Resource map [$(date +'%Y-%m-%d %H:%M')]"
            git push
            echo "✅ Changements commitées et pushées"
          fi
      
      - name: 🎉 Workflow completed
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🎸 Prof de Basse Auto-Indexer terminé!"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
          if [ -f "resources/stats.json" ]; then
            echo ""
            cat resources/stats.json
            echo ""
          fi
          
          echo "✅ Workflow réussi!"
