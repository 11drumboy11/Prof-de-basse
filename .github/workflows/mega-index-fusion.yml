name: 🔍 Mega Index Auto-Fusion

on:
  push:
    paths:
      - '**/*_index.json'
      - 'Prof-de-basse-OCR/**/*.json'
      - 'resources/**/*.json'
  workflow_dispatch:

jobs:
  mega-fusion:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: 🔍 Run Mega Fusion
        run: |
          echo "🚀 Démarrage fusion MEGA index..."
          python3 fusion-all-indexes.py --repo . --output mega-search-index.json
      
      - name: 📊 Display Statistics
        run: |
          echo "📈 Statistiques du MEGA index :"
          python3 -c "
          import json
          with open('mega-search-index.json', 'r', encoding='utf-8') as f:
              data = json.load(f)
              print(f\"✅ Total ressources: {data['total_resources']}\")
              print(f\"📚 Sources fusionnées: {data['statistics']['sources_merged']}\")
              print(\"📊 Par type:\")
              for t, count in data['statistics']['by_type'].items():
                  print(f\"   {t}: {count}\")
          "
      
      - name: 💾 Commit updated mega-index
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Ajouter le fichier
          git add mega-search-index.json
          
          # Commit uniquement si changements
          if git diff --staged --quiet; then
            echo "✅ Aucun changement dans le mega-index"
          else
            git commit -m "🔍 Auto-update: Mega index fusion [skip ci]"
            git push
            echo "✅ Mega-index mis à jour et pushé"
          fi
